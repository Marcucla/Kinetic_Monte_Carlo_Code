#include "../../StandardIncludes.h"

// The dummy variables nedded to initialize the linear algebra objects to default values.
NullVector NULL_VECTOR;
Origin ORIGIN;

/// The three unit vector.
Vector3 unitVectors[3] = {
		Vector3(1,0,0),
		Vector3(0,1,0),
		Vector3(0,0,1)
};
